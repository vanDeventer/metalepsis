# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*_rpi64	# ignore executables for the Raspberry Pi with 64bit OS
*_imac	# ignore executables for Intel Mac
*_amac	# ignore executables for ARM Mac
*_ilin	# ignore executables for Intel Linux
*.sh
Makefile

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
.DS_Store
.vscode/*

# AiGo
Systemconfig.json
go.mod
go.sum
serviceRegistry.db
*.pem
